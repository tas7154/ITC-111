// If-Else Statments - gives the ability to decide between different outcomes

/*
    Syntax: 
        if ( condition to meet ) { 
            code to run if true;
        } else {
            code to run if no true;
        }
*/

// Ex.

        // var condition = false;
        // if (condition) {
        //     console.log(true);
        // } else {
        //     console.log(false);
        // }

        var number = 5000;
        if(number < 30) {
            console.log('less than');
        } else {
            console.log('greater than');
        }
