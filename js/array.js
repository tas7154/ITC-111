// Arrays

// Arrays - group of elements/objects stored under the same.center

/*
    Syntax
            keyword <identifier> <assignment operator> '=' <[ elements of array ]>
*/

// sample string array
var fruits = ['apples', 'oranges', 'grapes', 'lemons', 'bananas'];

// array methods

// length method - getst the number of elements in an array.
    //console.log(fruits.length);

// push() method - adds new element to the end of an array.
   // console.log(fruits);
   // fruits.push('cantelop');
   // console.log(fruits);

// pop() method -  removes the last element in an array.
    // console.log(fruits);
    // fruits.pop('cantelop');
    // console.log(fruits);

// sort() method - sorts the elements in an array.
    // console.log(fruits);
    // console.log(fruits.sort());

// toString() method - convert an array to a string and return result.
    console.log(fruits);
    console.log(fruits.toString());



    Arrays

        Arrays allow you to store groups of objects under one name.


    array methods

        push() - adds new element to end of array
        pop() - removes the last element in array
        sort() - sorts the elements in array
        length() -  get the number of elements in array
        slice() -  removes a part of an aray and returns new array
        toStrint() - converts an array to a string and returns result