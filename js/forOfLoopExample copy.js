// ForOF Loop - used to iterate/loop throught elements in an array

// Syntax: for(element of array) {do stuff;}

var fruits = ['apple','orange','pear'];
var e;

for(e of fruits){   
    console.log(e);
}